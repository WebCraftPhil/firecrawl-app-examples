interface:
  display_name: "Firecrawl App Examples"
  short_description: "Repo-specific patterns and workflows for firecrawl-app-examples"
  default_prompt: "Use the firecrawl-app-examples repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true